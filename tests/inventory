# An inventory for testing the playbook; typically you will want this
# inventory to contain one host, `localhost` or `127.0.0.1` so that
# you can test and develop the playbook on your local dev environment.
localhost
