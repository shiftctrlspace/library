---
- import_playbook: "../playbooks/provision.yml"

- name: Stopgap for unfinished Lighttpd role integration.
  hosts: all
  become: true
  tasks:
    - name: Open port 80 for curl test.
      iptables:
        chain: INPUT
        protocol: tcp
        destination_port: 80
        jump: ACCEPT
