---
- name: Setup test container.
  hosts: localhost

  tasks:
    - name: Install packages.
      package:
        name: "{{ item }}"
        state: present
      loop:
        - git
        - openssh-server
        - netcat

